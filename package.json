{
  "name": "mi-dia-bitacora",
  "version": "3.0.0",
  "private": true,
  "description": "Bitácora diaria de bienestar por voz, con almacenamiento local y análisis opcional en servidor.",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check app.js"
  },
  "engines": {
    "node": ">=20"
  }
}
